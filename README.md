# JURRI

```
usage: jurri [-h] output

Generates a home page for JURRI

positional arguments:
  output      Output directory for the program. NB: Will be overwritten!

optional arguments:
  -h, --help  show this help message and exit
```
